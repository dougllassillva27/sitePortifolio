import type { ReactNode } from 'react';

interface ProvedoresAplicacaoProps {
  children: ReactNode;
}

export function ProvedoresAplicacao({ children }: ProvedoresAplicacaoProps) {
  return <>{children}</>;
}
